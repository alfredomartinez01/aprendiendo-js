//1041. Robot Bounded In Circle

/*
    - Si sabemos que regresa al punto de partida, entonces es verdadero 
    - Si solamente hay R´s o solamente hay L´s o ambas entonces siempre regresa al mismo punto 
    - Si solo hay G entonces no regresa al mismo punto
    
    - Si G es par entonces R o L tamién deben ser par
    - Si G es impar entonces R o L también deben ser impar
    
    LLLLLGR
    LLG
*/

/**
 * @param {string} instructions
 * @return {boolean}
 */

const isRobotBounded = function(instructions) {
    
};